frontend:
    image: node:0.10
    environment:
        - PORT=9000
    ports:
        - "9000:9000"
    links:
        - db
    volumes:
        - .:/usr/src/adminpanel
    working_dir: /usr/src/adminpanel
    command: node server.js

db:
    image: mongo:2.6
